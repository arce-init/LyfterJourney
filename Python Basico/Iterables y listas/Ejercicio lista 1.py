first_list = ['There', 'are', 'cases', 'where', 'indices', 'are']
second_list = ['very', 'useful', 'to', 'iterate', 'over', 'them']

for i in range(len(first_list)): 
    print(first_list[i], second_list[i])